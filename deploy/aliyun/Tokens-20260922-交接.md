# tokens 镜像部署记录与交接

## 当前状态

- 交付包：`C:\Users\Administrator\Documents\ChatGPT\大学院废墟中文卡图\server_updates\tokens-20260922-181200\forge-tokens-20260922-181200.zip`
- ZIP SHA-256：`7866baed5915ea5b21c5bccaabc04d949afc663d0f2999261430887858c18622`。
- 2,954张图的本地大小和SHA-256已全部核验：中文1,054张、英文1,900张；仅提取清单列出的图片到独立暂存目录。
- 初次上传因PutObject返回403 AccessDenied（EC 0003-00000001，bucket acl）停止，未因此切换客户端清单。管理员随后完成部署。
- 2026-09-22恢复发布工作后，逐张通过公网CDN GET核验2,954/2,954张图片，大小、SHA-256和JPEG文件头全部通过，包括15个含`☇`的路径；无需重复上传。校验报告位于`dist/diagnostics/tokens-cdn-verified.json`。
- 示例失败请求ID：`6AB2571388E4CC3733A950C5`。不要为此开放Bucket公共写权限。

## 部署路径及管理员操作参考

目标Bucket：`forge-cn-images-a8k3`。

目标对象前缀：`tokens/tokens-20260922-181200/`。

公网前缀：`https://images.mtg-forge-kaorou.vip/tokens/tokens-20260922-181200/`。

示例对象：`tokens/tokens-20260922-181200/2XM/1_c_0_1_eldrazi_spawn_sac.jpg`。

可以由管理员自行上传，也可为现有发布RAM身份授予此限定路径的`oss:PutObject`权限，并确认Bucket Policy等没有显式拒绝。最小对象Resource为：

`acs:oss:*:*:forge-cn-images-a8k3/tokens/tokens-20260922-181200/*`

不要在聊天中发送AccessKey或私钥。无需访问、修改或重启联机大厅ECS。

上传前确认该版本前缀尚未被其他内容占用。如有不同内容，先停止并另选新版本前缀；不要覆盖未知对象。只上传ZIP内`tokens/`的内容，保留其相对路径，不要额外嵌套一层`tokens`。不要上传JSON、脚本、ZIP等到图片根目录；不要删除服务器任何既有图片。

本地已经核验的暂存目录：

`C:\Users\Administrator\Documents\ChatGPT\mtg forge 汉化-upstream-sync-20260922\dist\tokens-20260922-181200\images`

使用有权限的ossutil身份执行（先上传单张核验权限，再批量）：

```powershell
ossutil cp '<暂存目录>\2XM\1_c_0_1_eldrazi_spawn_sac.jpg' 'oss://forge-cn-images-a8k3/tokens/tokens-20260922-181200/2XM/1_c_0_1_eldrazi_spawn_sac.jpg' --content-type image/jpeg --cache-control 'public,max-age=31536000,immutable'
ossutil cp '<暂存目录>\' 'oss://forge-cn-images-a8k3/tokens/tokens-20260922-181200/' --recursive --job 8 --content-type image/jpeg --cache-control 'public,max-age=31536000,immutable'
```

现有CDN需允许`/tokens/`路径使用同一个私有OSS回源。公网检查正常路径和包含`☇`的路径，后者UTF-8 URL编码为`%E2%98%87`；逐图校验以交付包manifest.json为准。不要改图片名、收藏编号或添加`.fullborder`。

## cn0922r3 客户端接入

- 候选版本cn0922r3；两端共用ImageFetcher，tokens独立配置`tokens.baseUrl`，优先社区镜像，继续保留token-images.txt原图源及Scryfall回退。
- 社区URL不执行旧图源的删后缀重试，防止误改图片编号或名称；普通卡图路径不变。
- 102项针对性回归测试通过；包括镜像优先级、原源保留、编号区别、Unicode及特殊字符编码。
- 66条缺图记录仍由原源尝试，另249张未被Forge登记的Scryfall印刷不在本次范围。
- 本地已有缓存不会被自动替换，不清空用户卡图缓存。
- 安卓APK位于`dist/android-cn0922r3/Forge-2.0.15-cn0922r3-Android.apk`，Windows包位于`dist/desktop-cn0922r3`，完整及增量安装包位于`dist/installers-cn0922r3`。两端打包配置已核对tokens地址，安卓签名保持不变，字库审计3,926个码点无缺字。安装包上传并完成CDN验证后才能切换清单，不能把指向403/404的候选版本发布给用户。
- 普通卡图的2,058张增量不在本次授权范围内，未处理。

后续可使用`publish-token-images.ps1`，默认只核验和暂存，加`-Upload`才上传；使用新的暂存目录，脚本不删除已有目录。已增加单图写入预检，权限失败时不继续批量请求。

公网验证使用`verify-token-cdn.ps1 -ManifestPath <manifest.json> -BaseUrl <版本目录HTTPS地址> -ReportPath <报告.json>`，8路并发、每请求30秒超时，失败最多再试一次；全量大小与SHA-256核验不通过即阻止发布。脚本只读公网图片，不更改服务器内容。
